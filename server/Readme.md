1 make a directory of Temporary chat
2 make separate directories of server and client
3 In the server folder init the node using " npm init -y "
4 add the dependencies using "npm install cors express socket.io"
5 add the nodemon using "npm install nodemon --save-dev"